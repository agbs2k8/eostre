{
  "name": "my-ui",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"pnpm --filter location dev\" \"pnpm --filter app2 dev\"",
    "build": "pnpm --recursive run build",
    "lint": "pnpm --recursive run lint",
    "test": "pnpm cypress run"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "cypress": "^13.15.0",
    "typescript": "^5.5.4"
  }
}
